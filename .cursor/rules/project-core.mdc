---
description: 碳足迹前端原型 — 仓库定位、业务边界、layout 同步与协作原则
alwaysApply: true
---

# 项目核心约定

## 定位

- 钢铁行业碳足迹数据服务的 **前端原型**：多页静态站点，无真实后端；演示与需求对齐为主。
- 业务与状态机以 `docs/` 为准，任务与列表 Mock 以 `js/mockTasks.js` 为准；不要在页面里发明与文档冲突的阶段或状态名。

## 任务与阶段（不可随意增删）

- 任务管理侧 **五段**：配置 → 采集（含审核）→ 计算（LCA）→ 核查 → **报告**。
- 阶段 4「报告」的流转、下发、归档等在 **报告管理** 完成，任务详情页不承载报告侧全量能力。详见 `docs/01_业务与流程/04_任务调度与状态机.md`。

## 工程易错点

- **`js/layout.js` 是唯一权威源**；`public/js/layout.js` 由 [`scripts/sync-public-js.js`](../../scripts/sync-public-js.js) 自动同步。`vite dev` / `vite build` 均通过 `vite.config.js` 的 `viteSyncPublicJsPlugin` 自动调用，也可手动 `npm run sync:public`。
- 因此**禁止直接编辑 `public/js/layout.js`**；提交前若不确定一致性，跑一次 `npm run sync:public` 即可。

```text
✅ 改 js/layout.js → 自动同步到 public/js/
❌ 直接改 public/js/layout.js（会被下一次同步覆盖）
```

## 语言与范围

- 界面文案、注释、协作说明默认使用 **简体中文**（与团队文档一致）。
- 仅改与需求相关的文件；无明确需求时不要新建大段文档或做大范围无关重构。
