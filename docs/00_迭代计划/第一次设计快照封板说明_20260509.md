# 第一次设计快照封板说明（全仓）

## 1. 封板目的

将 **2026-05-09** 当日仓库内**已纳入 Git 的全部路径**（见 §2）固化为 **产品经理设计交付的第一次快照（v1）**，作为需求/设计评审、客户对照与争议追溯的**只读基线**。  
**不替代**后续正式开发环境中的运行态与接口实现；引入开发环境后，**后续设计以开发环境所约定的仓库/分支/提交为事实源（SoT）**，本快照仅作 v1 对照。

## 2. 封板范围

以下目录与文件（以本仓库根为起点，且受 `.gitignore` 排除者除外）均属 v1 设计快照范围：

- `docs/`：业务、流程、功能、任务详情、澄清与消息、标准与合规及迭代计划等设计文档  
- `operator/`、`supplier/`、`certifier/`：多页原型 HTML  
- `js/`、`css/`、`public/`：布局、Mock、SpreadJS 工具与静态资源  
- 根目录：`index.html`、`pcf.html`、`vite.config.js`、`package.json`、`README.md`、`AGENTS.md` 等工程与入口说明  
- `.cursor/rules/`：Cursor 项目规则（团队与 AI 协作约定）  
- `客户商务/`：商务与建设方案相关材料（**对外分发须单独做脱敏与授权**）  

**不在 Git 对象内、但影响本地/演示运行的依赖**（如 SpreadJS、Vue、Element Plus 的 CDN 引用）以当时 [`README.md`](../../README.md) 与 [`docs/02_功能与对接/05_模板引擎解析逻辑.md`](../02_功能与对接/05_模板引擎解析逻辑.md) 的说明为准；封板不锁定 CDN 远端版本号。

## 3. 时点与 Git 对象

| 项 | 值 |
|----|-----|
| 封板日历日 | 2026-05-09 |
| 附注标签（annotated tag） | `design-freeze-v1-20260509` |
| 标签含义 | 指向「全仓内容 + 本封板说明」合并后的**单一提交**，即 v1 设计快照在 Git 中的可复现锚点 |

**查验（任一克隆本仓库的机器上）**：

```bash
git fetch --tags
git show design-freeze-v1-20260509 --no-patch
git rev-parse design-freeze-v1-20260509^{commit}
```

上述 `git rev-parse` 的输出即本快照对应的 **40 位提交哈希**（不在本文档内嵌该哈希，避免「文档自指提交」在 amend 时产生震荡；以 Git 标签解析结果为准）。

**可选只读分支**（与标签**不得同名**，避免 `refname is ambiguous`）：例如 `git branch snapshot/design-freeze-v1-20260509 design-freeze-v1-20260509`，便于 `git diff`。

## 4. 权威性与用途

- **业务规则与状态机**仍以 `docs/01_业务与流程`、`docs/02_功能与对接` 等目录下既有文档表述为准；本封板**不新增**业务规则，仅锁定**该时点的版本组合**。  
- 用于：与客户的「当时交付界面与文档」对齐、内部迭代对比、报价/范围回溯。  
- 若需离线包，可自行对标签检出目录打 zip，并在内部台账记录 zip 的 SHA256（非本仓库必选交付物）。

## 5. 后续开发环境基线（SoT）

公司内网已具备 **PCF 产品线的 GitLab 组与多仓实现**；**此后产品设计、联调与范围说明默认对照本节所列运行入口与代码仓**，本仓库 v1 设计快照（`design-freeze-v1-20260509`）仅作历史对照与需求溯源。

### 5.1 内网入口与 GitLab 实例

| 项 | 地址 / 说明 |
|----|-------------|
| GitLab 实例（用户/命名空间入口） | `http://192.168.11.226/sun.kangfeng` |
| PCF 组（组名 `blockchain-future`，组 ID **278**） | `http://192.168.11.226/blockchain-future` |
| 组内按 PCF 筛选（便于列出相关项目） | `http://192.168.11.226/blockchain-future?filter=pcf` |

上述页面为 **Private** 项目集合；访问需公司内网与 GitLab 账号权限。

### 5.2 组内六仓与 0209 原型目录对应

| GitLab 项目名（以界面为准） | 职责 | 与本原型仓库目录对应 |
|-----------------------------|------|------------------------|
| `pcf-center-fe` | 中心运营端前端 | [`operator/`](../../operator/) |
| `pcf-center-back` | 中心运营端后端 | 原型无后端；承载订单/任务/报告等 API |
| `pcf-supplier-fe` | 供应商工作台前端 | [`supplier/`](../../supplier/) |
| `pcf-supplier-back` | 供应商侧后端 | 同上 |
| `Pcf Audit Fe` | 核查/认证侧前端（名称大小写以 GitLab 为准） | [`certifier/`](../../certifier/) |
| `pcf-audit-back` | 核查/认证侧后端 | 同上 |

### 5.3 Clone 路径约定

内网 HTTP 克隆路径一般为（**以各项目页「Clone」按钮为准**，若组下还有子组则前缀可能更长）：

```text
http://192.168.11.226/blockchain-future/<项目名>.git
```

示例：`http://192.168.11.226/blockchain-future/pcf-center-fe.git`。

### 5.4 各仓基线（请负责人按仓补全）

以下字段**按仓库**维护，与部署/迭代起点对齐后填入；可附内部 wiki 链接替代长表。

| GitLab 项目名 | 默认分支 | 基线提交（baseline-commit） | 启用日期 | 负责人 |
|---------------|----------|----------------------------|----------|--------|
| pcf-center-fe | 待填 | 待填 | 待填 | 待填 |
| pcf-center-back | 待填 | 待填 | 待填 | 待填 |
| pcf-supplier-fe | 待填 | 待填 | 待填 | 待填 |
| pcf-supplier-back | 待填 | 待填 | 待填 | 待填 |
| Pcf Audit Fe | 待填 | 待填 | 待填 | 待填 |
| pcf-audit-back | 待填 | 待填 | 待填 | 待填 |

**查验各仓当前 HEAD**：在已 clone 的仓库内执行 `git rev-parse HEAD` 并与上表「基线提交」对齐后归档。

## 6. 变更纪律（建议）

- 重大范围或里程碑变更：走 [`迭代计划与排期说明`](迭代计划与排期说明.md) / [`设计落地执行方案`](设计落地执行方案.md) 中的既有节奏。  
- 开发环境启用后：需求/交互变更建议同步一条简短记录（可在迭代计划中增「设计变更」小节，或单独 `设计变更记录.md`），并注明相对 **开发基线提交** 的差异要点。

## 7. 关联索引

- 文档总索引：[docs/README.md](../README.md)  
- 工程说明：[README.md](../../README.md)
